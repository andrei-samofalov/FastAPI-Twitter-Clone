# test database

version: '3.9'
services:
  test_db:
    image: postgres:15.2
    container_name: test-postgres
    environment:
      - POSTGRES_USER=test
      - POSTGRES_PASSWORD=test
      - PGUSER=test
      - POSTGRES_DB=test
    ports:
      - '5434:5432'